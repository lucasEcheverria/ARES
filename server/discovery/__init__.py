"""Passive/semi-passive subnet device discovery (SDD §4).

A linear, deterministic backend process — not agent tools. Nothing here is
registered in `agent`'s `ToolRegistry` or invoked by the LLM.
"""
